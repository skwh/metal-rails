<% provide(:title, "Editing Album #{@album.title}") %>
<div class="content_header">
	<h2>Editing Album - <%= @album.title %></h2>
</div>
<div class="content_body">
	<%= form_for(@album) do |f| %>
		<p><%= f.label :title, "Album Title" %>: <%= f.text_field :title %></p>
		<p><%= f.label :description, "Album Description:" %>
		<p><%= f.text_area :description, size: "75x12" %></p>
		<p><%= f.label :page_id, "Catergory:" %><%= f.select(:page_id, options_for_select(pages_to_catergories,@album.parent_page.id)) %></p>
		<hr />
		<h4>Images: <%= f.add_nested_fields_link :images %></h4>
		<ol>
			<%= f.nested_fields_for :images, wrapper_tag: :li do |i| %>
				<div>
					<p><%= i.label :url, "Image URL" %>: <%= i.text_field :url %> | <%= link_to "Upload Image", "#", {id:"uploadImage#{(i.object.id.to_i-1)}",class:"uploadImage"} %> <input type="file" class="uploadImageInput" accept="image/*" style="display: none;" /><br />
					<%= i.label :subtitle %>:<br /><%= i.text_area :subtitle, size:"40x5" %><br />
					<%= i.remove_nested_fields_link 'Remove' %></p>
				</div>
			<% end %>
		</ol>
		<p><%= f.submit "Save Changes" %> | <%= link_to "Delete this album", destroy_album_path(@album) %></p>
	<% end %>
</div>